export enum ClubTypes {
    Private = 'private',
    Public = 'public',
}

export enum ClubOfferTypes {
    Prepaid = 'prepaid',
    PunchCard = 'punch',
    Free = 'free',
}

export enum ClubOfferPurchaseTypes {
    Unlimited = 'unlimited',
    Single = 'single',
}

export enum StatusCodes {
    SUCCESS = 200,
    CREATED = 201,
}